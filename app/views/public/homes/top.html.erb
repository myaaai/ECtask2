
 <div class="container mt-5">
      <div class="row">
       <div class="mx-auto">

<p>ようこそながのケーキへ<br>
このサイトはケーキ販売のECサイトになります。<br>
会員でない方も商品の閲覧ができますが、<br>
購入には会員登録が必要になります。</p>
</div>

 <div class="container mt-5">


<h4>新着商品</h4>

<div class="row">
          
                  <% @items.each do |item| %>
                   <div class="col-lg-3">
                   <%= link_to item_path(item) do %>
                            <% if item.image.attached? %>
                            <%= image_tag item.image, size: "250x170" %>
                            <% else %>
                            <%= image_tag 'no_image', size: "250x170" %>
                            <% end %> 
                            <% end %>
                        <%= link_to item.name, item_path(item) %>
                   ￥<%= (item.price * 1.1).to_i %>
                    
                   </div> 
        <% end %>
        
   <%= link_to "全ての商品を見る>", items_path, class: 'link-right' %>
</div>